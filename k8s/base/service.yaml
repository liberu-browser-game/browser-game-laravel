apiVersion: v1
kind: Service
metadata:
  name: browser-game
  namespace: browser-game
spec:
  selector:
    app: browser-game
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
